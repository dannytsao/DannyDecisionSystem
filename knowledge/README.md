# Knowledge

Store only durable, source-attributed knowledge cards here. Every card must record confidence and verification status using `templates/knowledge-card.md`. Create topic directories when the first approved card exists rather than pre-populating empty taxonomy.
